spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Akshityadav370/ecommerce-config-server
          username: Akshityadav370
          password:
          default-label: main
          tryMasterBranch: false     # ← Add this line (important in newer Spring Cloud)

server:
  port: 8888

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka